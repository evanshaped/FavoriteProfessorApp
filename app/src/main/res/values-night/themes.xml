<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.FavoriteProfessorApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
    </style>

    <style name="ButtonStyleDark" parent="Base.Theme.FavoriteProfessorApp">
        <item name="android:backgroundTint">@color/blue</item>
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="DividerStyleDark" parent="Base.Theme.FavoriteProfessorApp">
        <item name="android:divider">@color/blue</item>
        <item name="android:dividerHeight">2dp</item>
    </style>

    <style name="ProfessorTitleDark" >
        <item name="android:textColor">@color/black</item>
        <item name="android:background">@color/blue</item>
        <item name="android:textSize">36sp</item>
    </style>
</resources>